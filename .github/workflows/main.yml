on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
      
    - name: 🛠 Build Angular app
      run: |
        npm install -g @angular/cli
        npm install
        ng build
      
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: 54.36.91.62
        username: tiakiau
        password: ${{ secrets.ftp_password }}
        local-dir: dist  # This is the local directory to upload
        remote-dir: /home/tiakiau/www  # This is the remote directory on the server
 
