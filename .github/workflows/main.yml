on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
      
    - name: 🛠 Build Angular app
      run: npm install && ng build --prod
      
    - name: 📂 Sync files
      uses: ProjetTiaki/FTP-Deploy-Action@V1.0
      with:
        server: 54.36.91.62
        username: tiakiau
        password: ${{ secrets.ftp_password }}
        local-dir: dist  # This is the local directory to upload
        remote-dir: /public_html  # This is the remote directory on the server
 
